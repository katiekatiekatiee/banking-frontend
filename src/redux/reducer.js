function reducer(state = {goals: [], transactions: []}, action) {
    switch(action.type) {
        // case value:
        // break;
        default:
            return state;
    }
}

export default reducer;

